---

- name: "CREATE KEY PAIR DIRECTORY FOR AWS"
  file:
    path: "{{ temp_key_dir }}"
    state: directory

- name: "CREATE KEY PAIRS FOR AWS"
  shell: "ssh-keygen -t rsa -b 4096 -q -C 'Generated by Ansible for bootstrap' -f {{ temp_key }} -N ''"
  args:
    creates: "{{ temp_key }}"
