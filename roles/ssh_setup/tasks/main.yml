---

- name: "CREATE KEY PAIR DIRECTORY FOR CLOUD"
  file:
    path: "{{ temp_key_dir }}"
    state: directory

- name: "CREATE KEY PAIRS FOR CLOUD"
  shell: "ssh-keygen -t rsa -b 4096 -q -C 'Generated by Ansible for bootstrap' -f {{ temp_key }} -N ''"
  args:
    creates: "{{ temp_key }}"
